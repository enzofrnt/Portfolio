---
description: Running npm command
globs: 
---

# Never use npm

- Use `pnp` instead of `npm`
- Use `pnpm` command for every installation
- Can't use `npm` as this package manager use here is `pnpm`
